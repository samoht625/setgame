services:
  - type: web
    name: setgame-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npm run migrate
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: setgame-db
          property: connectionString
      - key: PORT
        value: 10000
    healthCheckPath: /api/health

  - type: web
    name: setgame-frontend
    env: static
    plan: free
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /m/*
        destination: /multiplayer.html
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: setgame-db
    plan: free
    databaseName: setgame
    user: setgame_user





